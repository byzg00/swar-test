import React, { memo } from 'react';

import { Wrapper } from './styled';

const Index = memo(() => (
    <Wrapper>
        111
    </Wrapper>
  ));
Index.displayName = 'Index';

export default Index;
